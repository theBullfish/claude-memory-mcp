# Local SQLite (default — works immediately, no signup needed)
# DATABASE_PATH=claude_memory.db

# Turso (hosted) — for multi-machine access
# TURSO_URL=libsql://your-db-name.turso.io
# TURSO_AUTH_TOKEN=your-auth-token

# Instance identifier (how this instance signs its writes)
# INSTANCE_ID=claude-code-pc1
